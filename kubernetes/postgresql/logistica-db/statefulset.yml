apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db-logistica
spec:
  serviceName: db-logistica
  replicas: 1
  selector:
    matchLabels:
      app: db-logistica
  template:
    metadata:
      labels:
        app: db-logistica
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: "nodo"
                    operator: In
                    values:
                      - "vm1"
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: logistica
            - name: POSTGRES_USER
              value: logistica
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: sat-secrets
                  key: DB_PASSWORD_LOGISTICA
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: 1Gi
