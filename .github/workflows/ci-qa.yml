name: CI/CD - QA

on:
  push:
    branches: [develop]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and Push Backend Images
        run: |
          for service in reportes validacion logistica; do
            docker build -t ${{ secrets.DOCKER_USERNAME }}/sat-$service:qa-latest backend/servicio-$service
            docker push ${{ secrets.DOCKER_USERNAME }}/sat-$service:qa-latest
          done

      - name: Build and Push Frontend
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/sat-frontend:qa-latest frontend
          docker push ${{ secrets.DOCKER_USERNAME }}/sat-frontend:qa-latest

      - name: Build and Push Nginx Gateway
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/sat-nginx-gateway:qa-latest nginx
          docker push ${{ secrets.DOCKER_USERNAME }}/sat-nginx-gateway:qa-latest

      - name: Deploy to KIND (QA)
        env:
          KUBECONFIG: ${{ secrets.KUBECONFIG_QA }}
        run: |
          for service in reportes validacion logistica; do
            kubectl set image deployment/$service $service=${{ secrets.DOCKER_USERNAME }}/sat-$service:qa-latest -n sat-qa
          done
          kubectl set image deployment/frontend frontend=${{ secrets.DOCKER_USERNAME }}/sat-frontend:qa-latest -n sat-qa
          kubectl set image deployment/nginx-gateway nginx-gateway=${{ secrets.DOCKER_USERNAME }}/sat-nginx-gateway:qa-latest -n sat-qa
